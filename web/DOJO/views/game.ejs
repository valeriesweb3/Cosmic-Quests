<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game</title>
    <style>
    body {
      background-image: url('/background.jpg'); /* ����ָ���˱���ͼƬ��·�� */
      background-size: cover;
      background-position: center;
      height: 100vh;
      margin: 0;
      text-align:center;
      justify-content: center;
      align-items: center;
      color:white;
      font-size: 24px;
      h1 {
        position: relative;
        top: 30%;
      }
      form {
        position: relative;
        top: 31%;
        font-size: 28px;
        div {
          display: flex;
          flex-wrap: wrap;
          flex-direction: column;
          div {
            display: block;
          }
        }
      }
    }
  </style>
    
</head>
<body>
    <h1><%= questionID+1 %>. <%= question %></h1>
    <form action="/submit" method="post">
      <div>
        <% for(let i=0; i<options.length; i++) { %>
          <div>
            <input type="radio" name="option" value="<%= options[i] %>">
            <label><%= options[i] %></label>
          </div>
        <% } %><br>
      </div>
      <input type="submit" value="Next" style="font-size: 24px;border-radius: 12px;padding: 0.5rem 1rem;">
      <!-- <label><%= score %></label> -->
    </form>
</body>
</html>
